# catdir
`catdir` is a console utility written in C++ that recursively lists all files within a specified directory and its nested subdirectories. The program includes switches for displaying help and version information. It's useful for viewing documents or source code directly from the command line without individually opening each file.
